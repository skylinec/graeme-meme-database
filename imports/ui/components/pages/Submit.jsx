import React, { Component, PropTypes } from 'react';

export default class Submit extends Component {
    constructor(){
        super(...arguments);
    }

    render() {
        return (
            <div>
                <h1>Submit</h1>
            </div>
        )
    }
}